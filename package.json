{
  "name": "construction-company-server",
  "version": "1.0.0",
  "description": "Backend server for construction company website",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "rimraf dist && mkdir -p dist",
    "start": "node dist/index.js",
    "dev": "nodemon src/index.ts",
    "build": "tsc --project tsconfig.json",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "mongoose": "^7.0.0",
    "rimraf": "^5.0.10"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^20.3.1",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
